apiVersion: v1
kind: Service
metadata:
    name: "bootcampakscluster-96ca"
    labels:
        app: "bootcampakscluster-96ca"
spec:
    type: LoadBalancer
    ports:
    - port: 6000
      targetPort: 6000
      protocol: TCP
      name: http
    selector:
        app: "bootcampakscluster-96ca"